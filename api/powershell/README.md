# PowerShell

This is where you can find PowerShell scripts related to the Anchore Enterprise API.

These scripts call the Anchore Enterprise REST API directly. Credentials should be loaded from environment variables or a configuration file and should never be hardcoded.
