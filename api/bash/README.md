# Bash

This is where you can find bash scripts related to the Anchore Enterprise API.

These scripts call the Anchore Enterprise REST API directly using `curl` and `jq`. Credentials are loaded from an `anchore.env` file and should never be hardcoded.
